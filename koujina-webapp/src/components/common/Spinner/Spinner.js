// src/components/Spinner/Spinner.js

import './Spinner.css'; // Styles for the spinner

import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
